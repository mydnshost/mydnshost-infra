---

version: '2'

services:
  web:
    environment:
      - API_PUBLICURL=https://api.somehost/
      - RECAPTCHA_SITE=
      - RECAPTCHA_SECRET=
    labels:
      com.chameth.vhost: "<VHOST>"

  api:
    environment:
      - HOOKKEY=
      - EMAIL_ENABLED=
      - EMAIL_SERVER=
      - EMAIL_USERNAME=
      - EMAIL_PASSWORD=
      - EMAIL_FROM=
    labels:
      com.chameth.vhost: "api.somehost"

  database:
    environment:
      - MYSQL_ROOT_PASSWORD=

  maintenance:
    environment:
      - HOOKKEY=
